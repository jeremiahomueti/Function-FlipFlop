//Arrow functions are a more modern way to write functions.

//Regular function to calculate the area of a circle:

const calcArea = function(radius)
{
    return 3.14 * radius ** 2;
}

const area = calcArea(5);
console.log('area is', area);

//Now see index2.js for arrow function version of same.